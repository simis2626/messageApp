/* tslint:disable:no-unused-variable */

import {AuthBoxComponent} from "./auth-box.component";

describe('Component: AuthBox', () => {
  it('should create an instance', () => {
    let component = new AuthBoxComponent();
    expect(component).toBeTruthy();
  });
});
